import React from "react";
import AccessibleSignInForm from "@/components/auth/AccessibleSignInForm";

/**
 * Page de connexion accessible
 * Utilise le formulaire optimisé pour les personnes âgées et handicapées
 */
const SignInPage = () => {
  return <AccessibleSignInForm />;
};

export default SignInPage;
